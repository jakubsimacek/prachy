extends layout

block content
  h2 Register
  if error
    h3 Missing #{error}

  //form(action="/register",method="post")
  form(action="/prachy/registrace",method="post")
    div
      label Username
      input(type="text",name="username")

    div
      label Password
      input(type="password",name="password")

    div
      input(type="submit",value="Register")

